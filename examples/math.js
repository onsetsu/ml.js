/*
 * sample data
 */
var vec = new ml.math.Vector([4, 3]);
var vec2 = new ml.math.Vector([6,-1]);

console.log(vec.length());
console.log(vec.scalarProduct(2));
console.log(vec.dotProduct(vec2));

